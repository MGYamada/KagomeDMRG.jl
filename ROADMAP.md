# 開発・研究ロードマップ

更新日: 2026-09-23。対象はスピン1/2の最近接・等方kagome反強磁性体、`M/Msat=1/9`。

**当面は研究計算の追加より、コードベースの機能・保守性・検証環境の充実を優先する。**
研究ごとに分かれた実行・診断処理を共通化し、公開API、保存契約、テストとCIを整える。
進捗は再利用できる機能と再現可能な検証で判断し、追加sweep数やテスト件数を目標にしない。
N54の固定χ256比較と既知CSLのN72準備は保留とし、開発の検証に必要な小系計算だけを行う。
研究対象・精度基準は維持し、VBC、磁気秩序、gapless状態、トポロジカルな液体を候補として残す。

## 現在地

| 対象 | 得られたもの | 未解決 |
| --- | --- | --- |
| 計算基盤（P0/P1/P3a） | 複素U(1) DMRG、格子・ゲージ・切断・保存・主要観測量の小系照合。共通静的診断・run契約と、sector energyからの磁化曲線解析API | CI・日常の検証時間と共通workflowの対象拡充は今後。磁化曲線用のrun集約とQ計算driverは未実装。大系の数値収束は各研究で評価する |
| 静的研究（P4a） | N18のED照合、N27の隣接磁化sector、N54の3準備比較とrandom／windmill同一親χ128/256・追加2 sweeps比較 | N54の新4子も全5精度条件が未達。中央差は緩和・χ増加で縮小し、優劣・bulk plateau・中性gapは未確立 |
| 輸送（P2/P3b/P4b） | flux継続の実装、零応答対照、N18の短区間照合 | 既知CSLの非零ポンプと1/9の量子化輸送は未検証 |

数値・失敗・精度判定は[静的研究の記録一覧](docs/research/p4_static_plateau_strategy.md#研究記録)と
[輸送の設計・検証範囲](docs/flux_insertion_design.md)に保存する。
既存のperiod9/27は周期に偏りを持たせた試行状態で、文献のVBC波動関数ではない。
新しいhourglass準備も文献の全bond階層を再現せず、共有頂点を持つbowtieを選択的に誘導する。
[N54初回比較](docs/research/p4_vbc54_preparation_comparison.md)に続く
[同一親χ比較](docs/research/p4_vbc54_matched_parent_comparison.md)を実装・実行した。
端2列除外のrandom／windmillの整列後bond RMS差は親で0.09153 J、χ128子で0.05082 J、
χ256子で0.03091 J。χ256でもrandom自身の親子変化0.05919 Jより小さく、
別々の安定した構造の証拠にはしない。分散・切断はχ増加で改善したが、精度は未達である。

## コードベースの優先順位

以下を開発の順序とする。保存後診断と零flux NNの設定・実行記録契約を実装し、対象拡充は開発計画である。
既存のsolver・保存再開・観測量・独立ED照合を土台に進める。

| 順位 | 整備するもの | 完了の判断 |
| --- | --- | --- |
| 1 | 共通の実行・診断基盤。`examples/`の個別driverから設定検証、実行状態の記録、保存後の診断を切り出す | 小系で最適化→trial保存→別process診断を再現でき、診断の欠測・失敗と数値精度未達を区別できる |
| 2 | APIとデータ契約。driverが使う内部`_checkpoint_*`等を棚卸しし、必要な設定・結果・由来情報の取得方法を文書化する | 共通driverが文書化したインターフェースを使い、入力エラー・保存形式の版・再開可能条件が明確になる |
| 3 | テスト・CI・利用例。既存の分野別テストをCIへ接続し、研究固定環境とライブラリ開発環境を区別する | 独立ED・QN切断・保存由来の回帰を維持し、対応Julia版と実行範囲・時間を記録する。最小例が実装と一致する |
| 4 | 計測に基づく性能改善。solver、分散、bond・相関・chirality、保存I/Oを別計時する | 同じ入力と物理精度で時間・メモリを比較し、支配的な処理を改善する。全相関行列を作るbond測定は候補の一つ |

GPU/MPIや独自backend（P5）はプロファイル後に必要性を判断する。
機能の共通化は既存研究の精度条件・受理条件を緩める理由にしない。

### 最初の開発単位（完了）

**静的計算の保存後診断を、研究ケースに依存しない共通APIへ切り出した。**
`research_static.jl`、`research_static_split.jl`、`research_csl.jl`の既存処理を確認し、
`static_diagnostics`と`diagnose_checkpoint`を実装した。
[小系driver・API契約・検証結果](docs/static_diagnostics.md)を整備し、
過去研究のdriverと記録は当時のまま保持する。

- 設定・診断結果・実行状態の契約を定め、完了trialの保存と別process診断を分けた。
- 同一source・環境で、直接測定と保存後の別process測定の一致を検証した。非零flux・磁場・非既定QとSchmidt cutを含む。
- 診断未実施・失敗・整合性不良・精度未達を区別し、再実行時にも親checkpointと元記録の不変性を検証した。
- 独立レビューと全体テスト1,855件を通過した。過去checkpointのsource・依存環境の厳密照合は維持した。

### 設定・実行記録の開発単位（零flux NNの範囲で完了）

**零flux NNの設定検証・solve記録・trial読込をライブラリの共通APIへ移した。**
`static_run_config`、`run_static`、`load_static_run`、`diagnose_static_run`を追加し、
小系exampleを共通API経由に切り替えた。[公開API・設定と保存契約](docs/static_workflow.md)を参照。

- 設定の許可キー・型・範囲を検証し、solver/modelの既定値を明示して保存する。小系予算はexample側に置く。
- trialとsolve記録の確定を診断から独立させ、直接診断が失敗しても完了済みsolveを読み込める。
- 完了recordのhash、実行時source・環境、設定と保存模型・solverの一致、保存物の一覧とhashを照合する。
- 診断出力は元runの外部に限定し、symlink経由の保存物参照も拒否する。旧recordやcheckpointの由来照合を緩めない。
- `run_dmrg`・保存・読込・診断の共有結果フィールドを文書化した。過去研究driverの一括移行は行っていない。

対象はランダム固定Q初期状態からの零flux NN（Jxy/Jz・縦磁場を指定可能）で、
拡張模型・準備状態・継続runの設定契約は今後。独立レビューと`workflow provenance`の
重点154件を通過した（2分39.9秒、[検証範囲](docs/static_workflow.md#validation-2026-09-23)）。
全体suiteと既存測定fixtureは今回重複実行していない。
次はCIの導入と全体テスト約5分からの日常検証時間の改善を優先する。
選択bond測定などの性能改善も未完了である。
N54/N72の新規計算や既存研究結果の再計算は行っていない。
共通処理・API・検証環境が整った段階で研究再開を判断し、保留項目を自動実行しない。
GPU対応や全開発候補の完了を研究再開の必須条件にはしない。

### 磁化曲線の最初の開発単位（解析APIの範囲で完了）

ユーザー提案により、固定Q計算を物理量へつなぐ解析APIを追加した。
`magnetization_curve`は、供給された全交換energy `E0(Q)` から
`E0(Q)-hQ/2` の下側包絡と `M/Msat=Q/N` を求める。
[API・TOML/CSV実行例](docs/magnetization_curve.md)を参照。

- 全入力sectorを競合させ、有限幅の区間、飛び越し、一点のみの縮退を区別する。
- 正規化したFloat64入力の有理数表現で比較し、sampleの近接判定許容差で区間を消さない。
- 未計算Qを列挙し、spin反転による補完を仮定しない。入力の精度ラベルは保持するが、数値収束・模型の一致・由来を認定しない。
- 検証は人工energy列とN9の独立EDに限定する。磁場ごとのDMRG、研究scan、N54/N72再開は行わない。

独立レビューと`magnetization`群の重点検証を通過した。最終`Pkg.test`呼出は約10.2秒。
全体suiteは重複実行していない。[実施した照合と限界](docs/magnetization_curve.md#validation-2026-09-23)。

この単位は追加DMRGを伴わない解析と小さな検証である。共通run記録の互換性を照合する
集約APIと、指定Qを計算するdriverは次の機能候補として残す。既存のCI・テスト費用改善も継続する。
有限系の磁化階段をbulk plateau・中性gapの確定とは扱わない。

## 保留中の研究単位

以下は研究再開時の候補であり、現在の実行指示ではない。

問いは **「χ256へ拡大した後の固定χ緩和でも、中央構造の差は縮小し続けるか」**。

- 対象: `(Lx,Ly)=(6,3), N=54, Q=6, θ=0`。軸OBC、wrap=`3a2`。
- 比較: randomとwindmill。初回の整列後bond差で選んだ2枝を保持し、trial energyで選び直さない。
- 親状態: 同一親比較で得たχ256・累積10-sweep checkpoint。準備項は除去済みで、全bondが最近接J=1。
- 継続: 各親からχ256固定で同じ追加2 sweeps、累積12へ進む2ケース。出自・依存環境をstrictに照合する。
- 測定: 中央bond・Sz・相関、全energyと既存の5精度条件。端1列／2列除外と円周並進を揃え、枝間差を各子の変化と比べる。
- 判断: 今回のχ増加を含む変化から固定χ緩和を切り分け、長さ・幅・隣接Qへ広げる前に残る構造差を評価する。

**初回の3準備比較と同一親χ128/256比較は実装・実行済み。次のχ256固定比較は未実装・未実行。**
次の2ケース全体のwall上限は3600秒、各子の2 sweeps後に一度全診断を測る案とする。
精度条件は緩和しない。この一単位の後も差が変動に埋もれる場合は、自動反復せずχ追加の費用・研究範囲を再判断する。
hourglass／windmillを収束した同一相としたわけではなく、現状で判別力のある2枝へ資源を集中する。

| 観測した結果 | 次に行うこと |
| --- | --- |
| 準備の違いを越えて中央の構造が一致する | 必要な長さ・幅を変え、その構造が残るか調べる |
| 異なる構造が残り、差が数値変動に埋もれる | 該当する状態のsweep／χ依存を調べ、優劣は保留する |
| 端の影響やdomain位置で差が説明できる | 端の処理・長さを変える。N81は必要な場合の次候補 |
| 資源内で識別できない | 自動反復を止め、資源追加・実装改善・狭幅研究への限定を判断する |

## 研究再開後にその結果から広げる

1. **秩序とサイズ依存。** 残った1〜2状態に長さ・幅の比較を集中する。Ly=3の長さ拡大だけで二次元相を確定しない。
2. **磁化励起と中性の性質。** 必要な隣接Qを選び、追加磁化が端か内部に入るかを見る。磁化区間・中性gap・秩序を相互に代用しない。
3. **輸送と相の識別。** 既知CSL校正を並行し、対象状態の評価と測定対照が揃ってから1/9ポンプを解釈する。秩序とトポロジーの共存も排除しない。

コード整備による保留とは別に、既知CSLの未達自体は静的研究を止める条件にしない。SU(3)1／D(Z3)は候補であり、
2/3ポンプを数値的な目標や状態の選択基準にしない。
[CSL保存結果の再監査](docs/research/p3_csl_resource_decision_20260920.md)では、N36・χ256の
追加sweepで分散・切断誤差が改善していない。機械的な同条件反復を止め、次は別資源単位で
N72=(6,4)・χ上限512の新規零flux準備の実行可能性を調べる案を保留項目として保持する。
[N72実行可能性監査](docs/research/p3_csl72_feasibility_20260922.md)で、必要な幾何選択・
段階的保存・診断分離と最大8時間の準備案を具体化した。新規driver・N72計算は未実装・未実行で、
実時間・メモリ・零flux精度と非零ポンプ校正は未確認である。

## 開発検証と研究計算を選ぶ方針

- 当面の計算は変更した機能の検証と、予算を定めた性能計測に限る。文書だけの変更ではDMRGを起動しない。
- 重点テストは約60秒、全体は約3分を目安とし、5分超では費用を調べる。許容差を緩めず、既存の独立参照・不具合回帰を維持する。
- 以下の研究計算の規則は再開時に適用する。
- 実行前に「解く不確かさ・ケース・得る観測量・結果による分岐」を定める。全サイズ×全χ×全seedを走査しない。
- 予算は物理比較の単位で定め、総wall上限・χ上限・測定頻度・途中保存を固定する。過去の600/900秒上限を恒久ルールにしない。
- 数値process一つ、Julia/BLAS各1 threadを出発点とする。fluxの同一軌道は逐次計算する。
- [精度基準](docs/research/p4_static_plateau_strategy.md#精度と解釈)は維持する。未達結果も探索に使うが、収束したenergy順位・境界・相とは主張しない。
- N27の追加精密化は、具体的な物理比較の判断を変える場合に行う。新規性や不具合がない基盤検証は繰り返さない。
- GPU/MPI・独自backend（P5）は必要な計算の費用を測ってから選ぶ。研究再開後の幅拡張と相同定（P6）は、その性能開発全体の完了を必須条件にしない。

## 文書の役割

| 文書 | 内容 |
| --- | --- |
| [README](README.md) | パッケージの入口、現在使える機能、最小実行例 |
| 本書 | 現在の開発優先順位、保留中の研究、再開・切替条件 |
| [研究方針の根拠](docs/research/research_direction_20260920.md) | 競合文献と2026-09-20時点の研究判断の理由 |
| [静的研究の規約](docs/research/p4_static_plateau_strategy.md) | 磁化境界の式、幾何、精度基準、研究記録 |
| [flux設計](docs/flux_insertion_design.md) | 電荷・ゲージ・輸送・候補理論の規約 |

新しい結果は個別の研究記録へ保存する。本書は現在地と次の判断を更新し、実行履歴を追記し続けない。
